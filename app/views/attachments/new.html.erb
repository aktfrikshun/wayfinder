<div class="page-head">
  <div>
    <h1>New Attachment</h1>
    <p>Create a source attachment record manually.</p>
  </div>
</div>

<section class="card">
  <%= render "form", attachment: @attachment %>
</section>
